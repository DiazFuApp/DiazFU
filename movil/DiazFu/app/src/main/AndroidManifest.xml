<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skillcoders.diazfu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/diazfu_logo_white"
        android:label="@string/app_name"
        android:roundIcon="@drawable/diazfu_logo_white"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NavigationDrawerActivity"
            android:label="@string/title_activity_navigation_drawer"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".MainRegisterActivity"
            android:label="@string/title_activity_navigation_drawer" />
        <activity
            android:name=".IntegrantesPlazosActivity"
            android:label="@string/title_activity_navigation_drawer" />

        <activity
            android:name=".DocumentosActivity"
            android:label="@string/title_activity_navigation_drawer" />
    </application>

</manifest>